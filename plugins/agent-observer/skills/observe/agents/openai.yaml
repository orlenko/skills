interface:
  display_name: "Observe agent work"
  short_description: "Run the local agent dashboard and review loop."
  default_prompt: "Use $agent-observer:observe to start the local dashboard and continuously review watched Claude and Codex sessions."
