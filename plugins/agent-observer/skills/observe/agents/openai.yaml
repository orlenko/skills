interface:
  display_name: "Observe agent work"
  short_description: "Start the dashboard and review local agent sessions."
  default_prompt: "Start Agent Observer, review the current project, and show me what needs attention."
