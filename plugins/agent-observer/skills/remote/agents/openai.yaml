interface:
  display_name: "Agent Observer Remote"
  short_description: "Connect or listen as a full Observer peer"
  default_prompt: "Use $agent-observer:remote to connect, resume, or listen as a full Observer peer and report transport, collection, and analyzer health."
